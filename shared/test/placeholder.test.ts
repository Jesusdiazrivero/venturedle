import { describe, expect, it } from "vitest";

// Placeholder so `vitest run` has something to do in this workspace before its phase lands.
describe("shared workspace", () => {
  it("is wired up", () => {
    expect(true).toBe(true);
  });
});
